# Pi-hole Group Toggle configuration

# Scheduled group actions
# Format:
#   '"<group>" <action> <OnCalendar>'
#
# "<group>"   : existing Pi-hole group name in double quotes; must be in db
# <action>    : enable | disable
# <OnCalendar>: systemd OnCalendar= value (passed through verbatim)

# Below are samples only - add/remove, rename groups, adjust times
# Each line is one timer action. Each entry must begin with exactly two spaces.

SCHEDULES=(
  '"Group Name Here" disable *-*-* 22:00:00'
  '"Group Name Here" enable  *-*-* 06:00:00'
  '"Another Group" disable *-*-* 17:00:00'
  '"Another Group" enable  *-*-* 08:00:00'
)
